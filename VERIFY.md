Please verify the following after unpacking:

- Update `configs/default.yaml` -> `data_path` to point to your dataset.
- Install dependencies from requirements.txt.
- Run: `python run_experiment.py --config configs/default.yaml`

If anything fails, tell me the error and I'll fix the files.
